{
	"name": "kids-db",
	"version": "5.0.2",
	"description": "Small file database for Node.js projects",
	"main": "index.js",
	"repository": "https://github.com/RusAnonym/kids-db.git",
	"author": "rus_anonym <alexandrsemin2033@gmail.com>",
	"license": "MIT",
	"directories": {
		"lib": "lib"
	},
	"scripts": {
		"build": "tsc -p tsconfig.json && tsc -p tsconfig-cjs.json",
		"test": "yarn build && node test.js",
		"prepublish": "yarn run build"
	},
	"files": [
		"lib/"
	],
	"dependencies": {
		"@shelacek/ubjson": "^1.0.1",
		"bson": "^4.2.0",
		"fs": "^0.0.1-security",
		"path": "^0.12.7"
	},
	"devDependencies": {
		"@types/node": "^14.11.10"
	}
}
